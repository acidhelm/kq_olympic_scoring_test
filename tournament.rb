class Tournament
    attr_accessor :brackets, :scenes

    # Reads the Challonge bracket with the given slug, and fills in all the
    # data structures that represent that bracket.
    def load(slug)
    end
end
